name: Flutter CI/CD with Shorebird

on:
  push:
    branches:
      - staging

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Flutter
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.29.3'
          channel: 'stable'

      # 3️⃣ Cache dependencies
      - name: Cache Flutter dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            ${{ github.workspace }}/.dart_tool
          key: ${{ runner.os }}-flutter-${{ hashFiles('pubspec.lock') }}

      # 4️⃣ Install dependencies
      - name: Install dependencies
        run: flutter pub get

      # 5️⃣ Setup Shorebird using official action
      - name: 🐦 Setup Shorebird
        uses: shorebirdtech/setup-shorebird@v1
        with:
          cache: true

      # 6️⃣ Manually login to Shorebird (fixes CI auth issue)
      - name: 🔐 Shorebird Login
        run: echo "${{ secrets.SHOREBIRD_AUTH_TOKEN }}" | shorebird login --token

      # 7️⃣ Verify Shorebird authentication
      - name: ✅ Verify Shorebird Auth
        run: |
          shorebird doctor
          echo "✅ Shorebird authentication successful"

      # 8️⃣ Check version change
      - name: Check version change
        id: version_check
        run: |
          VERSION_FILE="pubspec.yaml"
          CURRENT_VERSION=$(grep '^version:' $VERSION_FILE | awk '{print $2}')
          echo "CURRENT_VERSION=$CURRENT_VERSION" >> $GITHUB_ENV
          echo "Current version: $CURRENT_VERSION"
          
          # Fetch main branch to compare versions
          if git rev-parse --verify origin/main >/dev/null 2>&1; then
            git fetch origin main --depth=1
          else
            echo "No main branch found, skipping comparison."
            echo "VERSION_CHANGED=true" >> $GITHUB_ENV
          fi
          
          if git show origin/main:pubspec.yaml 2>/dev/null | grep -q '^version:'; then
            OLD_VERSION=$(git show origin/main:pubspec.yaml | grep '^version:' | awk '{print $2}')
            echo "OLD_VERSION=$OLD_VERSION" >> $GITHUB_ENV
            echo "Old version: $OLD_VERSION"
            
            if [ "$CURRENT_VERSION" != "$OLD_VERSION" ]; then
              echo "VERSION_CHANGED=true" >> $GITHUB_ENV
              echo "✅ Version changed from $OLD_VERSION to $CURRENT_VERSION"
            else
              echo "VERSION_CHANGED=false" >> $GITHUB_ENV
              echo "🔄 No version change detected"
            fi
          else
            echo "VERSION_CHANGED=true" >> $GITHUB_ENV
            echo "📝 First build or unable to compare versions, building release"
          fi

      # 9️⃣ Build or patch using Shorebird
      - name: Build or Patch
        run: |
          if [ "$VERSION_CHANGED" = "true" ]; then
            echo "🚀 Building new release (version: $CURRENT_VERSION)"
            shorebird release android --no-confirm --verbose
          else
            echo "🧩 Creating patch (no version change)"
            shorebird patch android --no-confirm --verbose
          fi

      # 🔟 Send to Telegram (only on new release)
      - name: Send APK to Telegram
        if: env.VERSION_CHANGED == 'true'
        run: |
          APK_PATH=$(find build -name "app-release.apk" -type f | head -n 1)
          if [ -z "$APK_PATH" ]; then
            APK_PATH=$(find . -name "*.apk" -type f | grep release | head -n 1)
          fi
          
          if [ -n "$APK_PATH" ] && [ -f "$APK_PATH" ]; then
            echo "📦 Sending $APK_PATH to Telegram..."
            curl -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                 -F document=@"$APK_PATH" \
                 -F caption="🚀 *New App Release v$CURRENT_VERSION*%0A📅 $(date '+%Y-%m-%d %H:%M')%0ABranch: staging%0ACommit: $GITHUB_SHA" \
                 -F parse_mode=Markdown \
                 https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument
          else
            echo "❌ APK file not found"
            echo "Searching for APK files..."
            find . -name "*.apk" -type f
          fi
