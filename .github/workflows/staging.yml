name: 🐦 Flutter CI/CD with Shorebird (iOS + Android)

on:
  push:
    branches:
      - staging
  workflow_dispatch:
    inputs:
      build_mode:
        description: "Build type"
        required: true
        default: "release"
        type: choice
        options:
          - debug
          - profile
          - release
      environment:
        description: "Environment"
        required: true
        default: "development"
        type: choice
        options:
          - development
          - production

env:
  SHOREBIRD_TOKEN: ${{ secrets.SHOREBIRD_AUTH_TOKEN }}
  FLUTTER_VERSION: '3.29.3'

jobs:
  build:
    runs-on: macos-latest
    defaults:
      run:
        shell: bash

    steps:
      # 1️⃣ Checkout the repository
      - name: 📚 Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      # 2️⃣ Setup Flutter SDK
      - name: 🐦 Setup Flutter SDK
        uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable

      # 3️⃣ Cache Flutter dependencies
      - name: 💾 Cache Flutter dependencies
        uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            ${{ github.workspace }}/.dart_tool
          key: ${{ runner.os }}-flutter-${{ hashFiles('pubspec.lock') }}

      # 4️⃣ Install dependencies
      - name: 📦 Install dependencies
        run: flutter pub get

      # 5️⃣ Setup Shorebird CLI
      - name: 🐥 Setup Shorebird CLI
        uses: shorebirdtech/setup-shorebird@v1
        with:
          cache: true

      # 6️⃣ Setup Java (for Android)
      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # 7️⃣ Setup Xcode (for iOS)
      - name: 🧰 Setup Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode.app/Contents/Developer
          xcodebuild -version

      # 8️⃣ Check version change
      - name: 🔍 Check version change
        id: version_check
        run: |
          VERSION_FILE="pubspec.yaml"
          CURRENT_VERSION=$(grep '^version:' $VERSION_FILE | awk '{print $2}')
          echo "CURRENT_VERSION=$CURRENT_VERSION" >> $GITHUB_ENV
          echo "📦 Current version: $CURRENT_VERSION"

          if git show HEAD~1:$VERSION_FILE >/dev/null 2>&1; then
            OLD_VERSION=$(git show HEAD~1:$VERSION_FILE | grep '^version:' | awk '{print $2}')
            echo "OLD_VERSION=$OLD_VERSION" >> $GITHUB_ENV
            echo "🕐 Previous version: $OLD_VERSION"

            if [ "$CURRENT_VERSION" != "$OLD_VERSION" ]; then
              echo "VERSION_CHANGED=true" >> $GITHUB_ENV
              echo "✅ Version changed from $OLD_VERSION ➜ $CURRENT_VERSION"
            else
              echo "VERSION_CHANGED=false" >> $GITHUB_ENV
              echo "🔄 No version change detected."
            fi
          else
            echo "VERSION_CHANGED=true" >> $GITHUB_ENV
            echo "📝 First commit or unable to compare versions."
          fi

      # 9️⃣ Run Shorebird Release or Patch (both iOS + Android)
      - name: 🚀 Create Shorebird Release or Patch (iOS + Android)
        run: |
          echo "📌 Flutter version: $FLUTTER_VERSION"
          echo "📦 App version: $CURRENT_VERSION"

          if [ "$VERSION_CHANGED" = "true" ]; then
            echo "🆕 Building new release for Android and iOS..."
            shorebird release android \
              --no-confirm --verbose \
              --artifact=apk \
              --flutter-version="$FLUTTER_VERSION"

            shorebird release ios \
              --no-confirm --verbose \
              --flutter-version="$FLUTTER_VERSION"
          else
            echo "🧩 Creating patch for both platforms..."
            shorebird patch \
              --platforms=android,ios \
              --release-version="$CURRENT_VERSION" \
              --no-confirm --verbose \
              --allow-native-diffs \
              --allow-asset-diffs
          fi

      # 🔟 Notify Telegram
      - name: 📤 Notify Telegram
        run: |
          if [ "$VERSION_CHANGED" = "true" ]; then
            echo "📤 Sending new release notification to Telegram..."
            APK_PATH=$(find build/app/outputs/flutter-apk -type f -name "*release*.apk" | head -n 1)
            IPA_PATH=$(find build/ios/ipa -type f -name "*.ipa" | head -n 1)

            MESSAGE="🚀 *New Release v$CURRENT_VERSION*%0A📅 $(date '+%Y-%m-%d %H:%M')"

            if [ -n "$APK_PATH" ]; then
              echo "✅ Found APK: $APK_PATH"
              curl -s -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                  -F document=@"$APK_PATH" \
                  -F caption="$MESSAGE (Android)" \
                  -F parse_mode=Markdown \
                  https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument
            fi

            if [ -n "$IPA_PATH" ]; then
              echo "✅ Found IPA: $IPA_PATH"
              curl -s -F chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
                  -F document=@"$IPA_PATH" \
                  -F caption="$MESSAGE (iOS)" \
                  -F parse_mode=Markdown \
                  https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendDocument
            fi

            if [ -z "$APK_PATH" ] && [ -z "$IPA_PATH" ]; then
              echo "❌ No build files found!"
            fi
          else
            echo "📩 Sending patch notification to Telegram..."
            curl -s -X POST "https://api.telegram.org/bot${{ secrets.TELEGRAM_BOT_TOKEN }}/sendMessage" \
              -d chat_id="${{ secrets.TELEGRAM_CHAT_ID }}" \
              -d text="🧩 *New Patch Created*%0A📦 Version: v$CURRENT_VERSION%0A📅 $(date '+%Y-%m-%d %H:%M')" \
              -d parse_mode="Markdown"
          fi
